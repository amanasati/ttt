#include<stdio.h>
#include<string.h>
struct Book {
    char tittle [1000];
    float price;
    int num copies : };
    int main () {
        struct book books [10];
        //* Reading data for 10 books *//
        for(int i=0; i<10; ++i) {
            printf("Entre details for book %d: \n", i+2);
            printf("Title");
            scanf("%s", books[i] . title );
            printf("Price:");
            scanf("%f", & books [i]. price );
            printf("Number of copies:");
            scanf("%d", & books[i]. num copies); }
        
    }
